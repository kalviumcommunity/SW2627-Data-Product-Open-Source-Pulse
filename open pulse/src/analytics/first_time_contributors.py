"""First-time contributor journey metrics (pandas/numpy)."""

def cohort_builder(journey_df):
    """Return a DataFrame with one row per first-time contributor."""
    ...

def return_rate_by_bucket(df, bucket_col="wait_bucket"):
    """Return rate per onboarding factor bucket."""
    ...

def return_rate_by_factor(df, factor):
    """Return rate grouped by any onboarding factor column."""
    ...
